/**
 * LargestNumberDetector
 * Finds the largest number in an array
 * @author ethanzhou
 *
 */
import java.util.Scanner;
public class largestNumber {
	static Scanner sc = new Scanner(System.in);
	public static void main(String[] args){
		System.out.println("Please input numbers, and the largest number will be found");
		int input = sc.nextInt();//*note need to get inputed numbers into the array, and make the array as much numbers as the user inputs
		for(int i = 0;)
		int[] array = new int[i];
	}
	public static
}
